# The 28 MB Sandia basemap archive is kept out of git (same policy as
# public/data/**/*.pmtiles). Run ios/scripts/sync-pack.sh to (re)copy it
# from the webapp's pack before building the app.
*.pmtiles
