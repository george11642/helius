// Text-to-speech: local voice output via kokoro-js, falling back to the
// browser's SpeechSynthesis API if kokoro's weights aren't loaded yet.
// TODO: implement speak(text) and wire it to the agent's response stream.

export {};
