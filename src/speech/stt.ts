// Speech-to-text: local, in-browser transcription (via
// @huggingface/transformers) so voice input never leaves the device.
// TODO: implement startListening()/stopListening(), streaming partial
// transcripts to the agent loop.

export {};
